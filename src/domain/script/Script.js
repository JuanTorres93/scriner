export class Script {
  constructor({ id, content, createdAt, title, userId }) {
    this.id = id;
    this.content = content;
    this.createdAt = createdAt;
    this.title = title;
    this.userId = userId;
  }
}
